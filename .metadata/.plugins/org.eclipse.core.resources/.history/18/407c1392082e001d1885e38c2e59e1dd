package org.ugp.api.chess.enginev2;


public class Bishop extends ChessPiece{
	public Bishop(SimpleChessEngine board, int color, int xLoc, int yLoc) {
		super(board, "b", color, xLoc, yLoc);
	}
	
	public boolean canMoveTo(int x, int y) {
		if (super.canMoveTo(x,y)) {
			return bishopMovement(x, y);
		}
		return false;
	}
	
	
	protected boolean bishopMovement(int x, int y) {
		return isMovingDiagonal(x, y);
	}
}