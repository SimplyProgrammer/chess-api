package org.ugp.api.chess.enginev2;

public class King extends ChessPiece{
	public King(SimpleChessEngine board, int color, int xLoc, int yLoc) {
		super(board, "K", color, xLoc, yLoc);
	}
	
	public boolean canMoveTo(int x, int yPosition) {
		if (super.canMoveTo(x,yPosition)) {
			return kingMovement(x, yPosition);
		}
		return false;
	}
	
	
	protected boolean kingMovement(int x, int yPosition) {
		int absoluteX = Math.abs(x - this.getX());
		int absoluteY = Math.abs(yPosition - this.getY());
		
		if (absoluteX <= 1 && absoluteY <= 1) {
			if (absoluteX == 0 && absoluteY == 0) {
				return false;
			}
			return true;
		}
		return false;
	}
}
